function flickSwitch(arr) {
  let state = true;
  let result = [];
​
  for (let item of arr) {
    if (item === "flick") {
      state = !state; // switch true ↔ false
    }
    result.push(state);
  }
​
  return result;
}
function flickSwitch(arr) {
  let state = true;
  let result = [];
​
  for (let item of arr) {
    if (item === "flick") {
      state = !state; // switch true ↔ false
    }
    result.push(state);
  }
​
  return result;
}
function flickSwitch(arr) {
  let state = true;
  let result = [];
​
  for (let item of arr) {
    if (item === "flick") {
      state = !state; // switch true ↔ false
    }
    result.push(state);
  }
​
  return result;
}


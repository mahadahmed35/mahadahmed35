function animal(obj){
  return `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;
}
​
​function animal(obj){
  return `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;
}
​
​
function animal(obj){
  return `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;
}
​
​
function animal(obj){
  return `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;
}
​function animal(obj){
  return `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;
}
​
​
​function animal(obj){
  return `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;
}
​
​
